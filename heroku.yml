setup:
  addons:
    - plan: heroku-postgresql
build:
  docker:
    web: Dockerfile
release:
  image: web
run:
  web: java -Dserver.port=$PORT -Djava.security.egd=file:/dev/./urandom -jar memogram-apps/memogram-app-postgres/target/memogram-app-postgres-0.0.1-SNAPSHOT.jar